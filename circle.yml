machine:
  java:
    version: openjdk8
test:
  post: # PlantUMLのクラス図を保存
    - ./.purc/plantuml-reverce-class-diaglam.sh
    - cp -r ./.purc/class-diagrams $CIRCLE_TEST_REPORTS/$CIRCLE_PROJECT_REPONAME/